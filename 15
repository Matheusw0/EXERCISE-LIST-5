matriz = [[13, 1, 9, 2006, 3, 1, 21, 9, 2]]
maior = matriz[0][0]
menor = matriz[0][0]
for linha in matriz:
    for elemento in linha:
        if elemento > maior:
            maior = elemento
        if elemento < menor:
            menor = elemento
print(f"Maior elemento: {maior}")
print(f"\nMenor elemento: {menor}")
